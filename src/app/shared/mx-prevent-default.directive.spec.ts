import { MxPreventDefaultDirective } from './mx-prevent-default.directive';

describe('MxPreventDefaultDirective', () => {
  it('should create an instance', () => {
    const directive = new MxPreventDefaultDirective();
    expect(directive).toBeTruthy();
  });
});
