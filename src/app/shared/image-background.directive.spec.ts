import { ImageBackgroundDirective } from './image-background.directive';

describe('ImageBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
